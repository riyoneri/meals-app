import { StyleSheet, Text } from "react-native";

export default function FavoritesScreen() {
  return <Text style={styles.text}>This is a favorite screen</Text>;
}

const styles = StyleSheet.create({
  text: {
    color: "white",
  },
});
